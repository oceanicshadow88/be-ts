// import config from '../config/app';
// import { tenantsDBConnection } from '../database/connections';

// export const getTenantDBbyId = async (domain: string, tenantId: string) => {
//     const tenantsConnection = await tenantsDBConnection();
//     const tenant = await getTenant(domain, tenantsConnection, isLocalEnv);

// }
